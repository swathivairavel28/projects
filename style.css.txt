/* style.css - Common styles for Daily Motivation Project */

body {
  margin: 0;
  font-family: 'Roboto', sans-serif;
  background: linear-gradient(90deg, #3c2a2a, #5d4037);
  color: #fff;
}

header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px 60px;
  background-color: rgba(0, 0, 0, 0.4);
}

header h1 {
  font-family: 'Playfair Display', serif;
  font-size: 28px;
}

nav a {
  color: #fff;
  text-decoration: none;
  margin-left: 30px;
  font-size: 16px;
}

button {
  background-color: #d1a684;
  color: #000;
  border: none;
  padding: 15px 30px;
  font-size: 16px;
  cursor: pointer;
  border-radius: 5px;
}

@media (max-width: 768px) {
  header {
    flex-direction: column;
    text-align: center;
  }

  nav {
    margin-top: 10px;
  }

  nav a {
    margin: 10px;
  }
}

.section {
  padding: 80px 60px;
}

.section-title {
  font-size: 40px;
  font-family: 'Playfair Display', serif;
  margin-bottom: 20px;
  text-align: center;
}

.section-description {
  font-size: 18px;
  max-width: 700px;
  margin: 0 auto 40px;
  text-align: center;
  line-height: 1.6;
}
